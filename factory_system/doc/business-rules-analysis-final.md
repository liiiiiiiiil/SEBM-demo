# 业务规则与状态分析报告（最终版）

**分析时间**：2026-01-14  
**分析角色**：业务 Agent  
**项目状态**：半成品（开发中，已优化）  
**分析目的**：梳理业务规则与状态、识别TODO与潜在歧义、提出补充规则建议

---

## 一、业务规则与状态描述

### 1.1 销售订单流程

#### 1.1.1 订单状态流转规则

**状态定义**：
- `pending`：待审批（销售员创建后）
- `approved`：已审批（销售经理审批后，但此状态实际不会停留，直接进入下一状态）
- `ceo_pending`：待总经理审批（销售经理审批后）
- `ceo_approved`：总经理已审批（总经理审批后，但此状态实际不会停留，直接进入下一状态）
- `rejected`：已退回（销售经理或总经理退回）
- `in_production`：生产中（总经理审批后，库存不足，创建了生产任务）
- `ready_to_ship`：待发货（总经理审批后，库存充足，创建了发货通知单）
- `shipped`：已发货（物流确认发货后）
- `completed`：已完成（所有发货单都送达后）
- `cancelled`：已取消（销售员在待审批状态下取消）
- `terminated`：已终结（总经理终结订单及关联流程）

**状态流转规则**：
1. **创建订单**：销售员创建 → `pending`
2. **销售经理审批**：
   - 审批通过 → `ceo_pending`（跳过`approved`状态）
   - 审批退回 → `rejected`
3. **总经理审批**：
   - 审批通过 → 自动触发库存研判，根据结果进入 `in_production` 或 `ready_to_ship`
   - 审批退回 → `rejected`
4. **库存研判后**：
   - 库存充足 → `ready_to_ship`（创建发货通知单）
   - 库存不足 → `in_production`（创建生产任务）
5. **生产完成后**：如果所有生产任务完成，且库存充足 → `ready_to_ship`
6. **发货后**：`shipped`
7. **所有发货单送达后**：`completed`
8. **终结**：总经理可在多个状态下终结订单，会回滚库存

**已优化规则**：
- ✅ 总经理审批时锁定批次库存（`locked_quantity`）
- ✅ 订单终结时完整回滚库存（已发货订单重新入库创建新批次，已入库成品扣减，已发料原料重新入库）

#### 1.1.2 订单审批规则

**销售经理审批**：
- 权限：`sales_mgr` 或 `ceo` 角色
- 可审批状态：仅 `pending`
- 审批后：进入 `ceo_pending` 状态

**总经理审批**：
- 权限：`ceo` 角色
- 可审批状态：仅 `ceo_pending`
- 审批前：系统会进行库存预判（不实际创建任务）
- 审批后：
  - 锁定批次库存（`batch.locked_quantity += order_batch.quantity`）
  - 自动触发 `check_inventory_and_create_tasks()` 函数

**退回规则**：
- 销售经理可退回 `pending` 状态的订单
- 总经理可退回 `ceo_pending` 状态的订单
- 退回后状态变为 `rejected`，销售员可修改后重新提交

#### 1.1.3 库存研判规则（`check_inventory_and_create_tasks`）

**研判逻辑**：
1. 遍历订单所有明细项
2. 对每个明细项：
   - 计算已分配的批次数量总和（从`SalesOrderItemBatch`）
   - 计算缺口 = 订单数量 - 批次分配总和
   - 如果缺口 > 0：
     - 检查原材料是否充足（根据BOM计算）
     - 创建生产任务，状态为 `pending`（原材料充足）或 `material_insufficient`（原材料不足）
3. 如果所有明细项批次分配都充足：
   - 创建发货通知单（`ShippingNotice`）
   - 订单状态 → `ready_to_ship`
4. 如果存在缺口：
   - 订单状态 → `in_production`

**批次分配规则**：
- 批次分配在总经理审批前进行（在`ceo_approve`视图的GET请求中）
- 使用FIFO原则分配批次
- 批次分配记录在`SalesOrderItemBatch`中
- 总经理审批时锁定批次库存（`locked_quantity`）
- 库存扣减在发货时进行（从锁定数量中扣减）

#### 1.1.4 订单终结规则（`terminate_order_chain`）

**终结条件**：
- 权限：`ceo` 角色
- 可终结状态：`in_production`、`ready_to_ship`、`shipped`

**终结操作**（已优化）：
1. **已发货订单重新入库**：
   - 创建新批次（批次号格式：`RETURN-{原订单号}-{日期}`）
   - 批次数量 = 订单数量
   - 更新库存总数量
   - 记录库存变动（类型为`adjustment`）

2. **扣减已入库的成品库存**：
   - 如果订单未发货但成品已入库，按批次FIFO扣减
   - 从最早入库的批次开始扣减
   - 记录库存变动（类型为`adjustment`，数量为负数）

3. **已发料原料重新入库**：
   - 查找所有已批准（已发料）的领料单
   - 对每个领料单明细项，创建新批次（批次号格式：`RETURN-{原订单号}-{原料SKU}-{日期}`）
   - 批次数量 = 领料单明细的需求数量
   - 更新库存总数量
   - 记录库存变动（类型为`adjustment`）

4. **释放锁定的批次库存**：
   - 如果订单状态是`ready_to_ship`、`ceo_approved`或`in_production`，释放锁定的批次库存
   - 从`batch.locked_quantity`中扣减

5. **终结所有关联流程**：
   - 终结所有关联的生产任务（状态 → `terminated`）
   - 终结所有关联的领料单（状态 → `terminated`）
   - 订单状态 → `terminated`

---

### 1.2 生产任务流程

#### 1.2.1 生产任务状态流转规则

**状态定义**：
- `pending`：待接收（创建后，原材料充足）
- `material_insufficient`：原料不足（创建时或接收时发现原材料不足）
- `received`：已接收（此状态在代码中定义但实际不会停留）
- `material_preparing`：备料中（此状态在代码中定义但实际不会停留）
- `in_production`：生产中（任务接收后直接进入此状态）
- `qc_checking`：质检中（生产完成后）
- `completed`：已完成（入库后，完成数量 >= 需求数量）
- `cancelled`：已取消
- `terminated`：已终结

**状态流转规则**：
1. **创建任务**：
   - 原材料充足 → `pending`
   - 原材料不足 → `material_insufficient`
2. **接收任务**：
   - 检查原材料是否充足
   - 如果不足 → `material_insufficient`
   - 如果充足 → 直接进入 `in_production`（跳过`received`和`material_preparing`状态）
   - 自动创建领料单，状态为 `pending`，需要仓库管理员审批
3. **生产完成**：`in_production` → `qc_checking`
4. **质检完成**：质检记录创建后，状态仍为 `qc_checking`（可多次质检）
5. **入库**：创建入库单后，如果完成数量 >= 需求数量 → `completed`
6. **任务完成后**：如果是订单生产，检查订单是否可以发货

**已优化规则**：
- ✅ 任务接收时不再自动扣减库存，而是创建领料单（`status='pending'`），需要仓库管理员审批
- ✅ 领料单审批时按批次FIFO扣减库存，使用`get_available_quantity()`方法（考虑锁定数量）

#### 1.2.2 领料单流程规则

**状态定义**：
- `pending`：待审核
- `approved`：已批准
- `issued`：已发料（此状态在代码中定义但实际不使用）
- `cancelled`：已取消
- `terminated`：已终结

**状态流转规则**：
1. **创建领料单**：
   - 任务接收时自动创建，状态为 `pending`
   - 需要仓库管理员审批
2. **审批领料单**：
   - 仓库管理员审批 → `approved`
   - 审批时按批次FIFO扣减库存（从可用数量中扣减，考虑锁定数量）
   - 任务状态保持 `in_production`
3. **发料**：`issued`状态在代码中定义但实际不使用

**已优化规则**：
- ✅ 任务接收时创建领料单，状态为`pending`，需要仓库管理员审批
- ✅ 领料单审批时按批次FIFO扣减库存，使用`get_available_quantity()`方法

#### 1.2.3 质检规则

**质检记录创建**：
- 权限：`qc` 或 `ceo` 角色
- 可在任务状态为 `in_production` 或 `qc_checking` 时创建
- 一个任务可以有多条质检记录

**质检结果**：
- `qualified`：合格
- `unqualified`：不合格
- `rework`：返工

**质检后处理**：
- 如果结果为 `qualified`：任务状态保持 `qc_checking`，可以入库
- 如果结果为 `unqualified` 或 `rework`：只显示警告消息，任务状态不变

**潜在歧义**：
- ⚠️ 质检不合格时，任务状态如何处理？是否需要重新生产？
- ⚠️ 返工（`rework`）的具体流程是什么？是否需要创建新的生产任务？
- ⚠️ 质检记录与入库单的关联关系：入库单可以关联质检记录，但不是必须的，规则未明确

#### 1.2.4 成品入库规则

**入库条件**：
- 权限：`warehouse` 或 `ceo` 角色
- 任务状态：`qc_checking`（理论上，但代码中未强制检查）
- 可以多次入库，直到完成数量 >= 需求数量

**入库操作**：
1. 创建入库单（`FinishedProductInbound`）
2. 创建批次（`Batch`）：
   - 批次号可以手动输入或自动生成
   - 批次日期默认为今天
   - 批次单价默认为产品基础单价
   - 过期日期可选
3. 增加成品库存（从批次汇总）
4. 记录库存变动（类型为`production_in`）
5. 更新任务完成数量
6. 如果完成数量 >= 需求数量，任务状态 → `completed`
7. 如果任务完成且是订单生产，检查订单是否可以发货

---

### 1.3 采购任务流程

#### 1.3.1 采购任务状态流转规则

**状态定义**：
- `pending`：待审批
- `approved`：已审批
- `purchasing`：采购中（此状态在代码中定义但实际不会停留）
- `completed`：已完成
- `cancelled`：已取消
- `terminated`：已终结

**状态流转规则**：
1. **创建任务**：`pending`
2. **审批**：`pending` → `approved`（权限：`ceo`）
3. **完成采购**：`approved` 或 `purchasing` → `completed`（权限：`warehouse` 或 `ceo`）
4. **终结**：`pending`、`approved`、`purchasing` → `terminated`（权限：`ceo`）

**已优化规则**：
- ✅ 支持部分收货（每次收货时更新`received_quantity`，只有当所有明细项都收货完成时，任务状态才变为`completed`）

#### 1.3.2 采购收货与入库规则

**完成采购操作**：
- 权限：`warehouse` 或 `ceo` 角色
- 可操作状态：`approved` 或 `purchasing`
- 操作内容：
  1. 对每个采购明细项：
     - 更新已收货数量（`received_quantity`）
     - 创建批次（`Batch`）
     - 增加库存（从批次汇总）
     - 记录库存变动（类型为`purchase_in`）
  2. 检查是否全部收货：
     - 如果所有明细项的`received_quantity` >= `quantity`，任务状态 → `completed`
     - 否则，任务状态 → `purchasing`

**批次创建规则**：
- 批次号可以手动输入或自动生成
- 批次日期默认为今天
- 批次单价从采购明细中获取
- 过期日期可选
- 供应商信息存储在批次备注中

---

### 1.4 物流发货流程

#### 1.4.1 发货单状态流转规则

**状态定义**：
- `pending`：待发货
- `loading`：装车中（此状态在代码中定义但实际不会停留）
- `shipped`：已发货
- `delivered`：已送达

**状态流转规则**：
1. **创建发货单**：从发货通知单创建，状态为 `pending`
2. **分配司机和车辆**：状态仍为 `pending`
3. **确认发货**：`pending` → `shipped`（权限：`logistics` 或 `ceo`）
4. **确认送达**：`shipped` → `delivered`（权限：`logistics` 或 `ceo`）

**已优化规则**：
- ✅ 发货前检查批次库存是否充足（计算所有批次的可用数量总和）
- ✅ 如果批次库存不足，不允许发货，提示用户
- ✅ 发货时从锁定数量中扣减（`batch.locked_quantity`）

#### 1.4.2 发货确认规则

**发货确认操作**：
- 权限：`logistics` 或 `ceo` 角色
- 可操作状态：`loading`（理论上，但实际是`pending`）
- 操作内容：
  1. **发货前检查**（已优化）：
     - 计算所有批次的可用数量总和
     - 如果批次库存不足，不允许发货，提示用户
  2. 对每个订单项，按批次FIFO扣减库存：
     - 优先使用订单中已保存的批次分配
     - 允许在发货时调整批次分配数量
     - 如果仍然不足，使用FIFO自动分配
     - 从锁定数量中扣减（`batch.locked_quantity`）
     - 从总数量中扣减（`batch.quantity`）
  3. 记录库存变动（类型为`sale_out`）
  4. 更新库存总数量（从批次汇总）
  5. 发货单状态 → `shipped`
  6. 发货通知单状态 → `shipped`
  7. 订单状态 → `shipped`

#### 1.4.3 收货回执规则

**收货回执操作**：
- 权限：`logistics` 或 `ceo` 角色
- 可操作状态：`shipped`
- 操作内容：
  1. 录入收货人信息（姓名、电话）
  2. 录入收货备注
  3. 上传发货回执图片（可选，可多张）
  4. 发货单状态 → `delivered`
  5. 检查订单的所有发货单是否都已送达
  6. 如果所有发货单都已送达，订单状态 → `completed`

---

### 1.5 库存管理规则

#### 1.5.1 库存类型

**库存类型定义**：
- `product`：成品库存
- `material`：原料库存
- `other`：其它类型库存

**库存数量计算**：
- 库存总数量从批次汇总计算（`update_quantity_from_batches()`）
- 批次数量变化时，需要更新库存总数量

#### 1.5.2 批次管理规则

**批次创建场景**：
1. 成品入库时创建批次
2. 采购收货时创建批次
3. 订单终结时重新入库创建批次（已优化）

**批次扣减规则**（已优化）：
- 领料单审批时：按批次FIFO扣减（从可用数量中扣减，考虑锁定数量）
- 发货确认时：按批次FIFO扣减（从锁定数量中扣减）
- 订单终结时：按批次FIFO扣减（从可用数量中扣减）

**批次锁定规则**（已优化）：
- 总经理审批订单时锁定批次库存（`batch.locked_quantity += order_batch.quantity`）
- 发货时从锁定数量中扣减
- 订单取消或终结时释放锁定数量

**批次信息**：
- 批次号：可手动输入或自动生成
- 批次日期：默认为今天
- 批次单价：从产品/原料基础单价获取，可手动调整
- 过期日期：可选
- 供应商信息：存储在备注中
- 锁定数量：`locked_quantity`字段（已优化）

**批次可用数量计算**：
- `get_available_quantity()` = `quantity` - `locked_quantity`
- 用于领料单审批和发货确认时的库存检查

---

### 1.6 客户管理规则

#### 1.6.1 客户编辑/删除审批规则

**编辑审批流程**：
- 申请编辑：任何角色可以申请
- 审批编辑：`ceo` 角色
- 状态：`none` → `pending` → `approved` / `rejected`

**删除审批流程**（已优化）：
- 申请删除：任何角色可以申请
- 审批删除：`ceo` 角色
- 状态：`none` → `pending` → `approved` / `rejected`
- **删除规则**（已优化）：
  - 如果客户有关联订单，执行软删除（`is_deleted=True`）
  - 如果客户无关联订单，执行硬删除（`customer.delete()`）

#### 1.6.2 客户转移规则

**客户转移**：
- 可以转移客户负责人
- 记录转移历史（`CustomerTransfer`）

---

## 二、TODO与潜在歧义

### 2.1 核心流程歧义

#### 2.1.1 质检不合格处理规则缺失 ⚠️ **流程歧义**

**歧义描述**：
- 质检结果有`qualified`（合格）、`unqualified`（不合格）、`rework`（返工）三种
- 但只有`qualified`有明确的后续流程（可以入库）
- `unqualified`和`rework`的处理规则未明确
- 当前代码：质检不合格时，任务状态保持`qc_checking`，只显示警告消息

**影响范围**：
- 质检流程
- 生产任务状态流转
- 不合格品处理

**是否阻塞实现**：⚠️ **部分阻塞**
- 功能上可以运行，但业务逻辑不完整
- 质检不合格后无法继续流程
- 可能导致任务卡在`qc_checking`状态
- 无法处理不合格品和返工流程

**行业默认做法**：
- ✅ **质量管理标准**：
  - `unqualified`（不合格）：需要报废或降级处理，创建"不合格品处理单"
  - `rework`（返工）：需要重新生产，任务状态应变为`in_production`，不需要重新领料（原料已领用）
- ✅ **ERP系统做法**：
  - 不合格品创建"不合格品处理单"，记录报废或降级原因
  - 返工任务状态变为`in_production`，允许继续生产

**建议**：
- **中优先级修复**：补充质检不合格处理规则
- `unqualified`：任务状态保持`qc_checking`，需要创建"不合格品处理单"（可选功能）
- `rework`：任务状态变为`in_production`，允许继续生产（不需要重新领料）

---

### 2.2 边界条件歧义

#### 2.2.1 订单退回后重新提交规则缺失 ⚠️ **边界条件歧义**

**歧义描述**：
- 订单被退回后，销售员可以修改后重新提交
- 但规则未明确：
  - 是否保留原订单号？
  - 是否保留审批历史？
  - 重新提交是否需要重新走完整审批流程？

**影响范围**：
- 订单退回流程
- 订单修改流程
- 审批历史追踪

**是否阻塞实现**：❌ **否，不阻塞实现**
- 功能上可以运行，但用户体验不明确
- 可能导致业务人员困惑
- 不影响核心业务流程

**行业默认做法**：
- ✅ **ERP系统标准做法**：
  - 保留原订单号（订单号是唯一标识）
  - 保留审批历史（用于审计追踪）
  - 重新提交需要重新走完整审批流程
- ✅ **业务最佳实践**：订单退回后，修改内容需要重新审批

**建议**：
- **低优先级修复**：明确订单退回后重新提交规则
- 保留原订单号
- 保留审批历史（`approved_by`、`approved_at`等字段保留）
- 重新提交后，状态从`rejected`变为`pending`，需要重新走完整审批流程
- 清除之前的审批信息（`approved_by`、`approved_at`等字段清空）

---

#### 2.2.2 入库数量超需求处理规则缺失 ⚠️ **边界条件歧义**

**歧义描述**：
- 生产任务有`required_quantity`（需求数量）和`completed_quantity`（完成数量）
- 规则未明确：如果`completed_quantity` > `required_quantity`，如何处理？
- 当前代码：允许超产，任务状态仍为`completed`

**影响范围**：
- 成品入库流程
- 生产任务完成判断
- 库存管理

**是否阻塞实现**：❌ **否，不阻塞实现**
- 功能上可以运行（当前代码允许超产）
- 但业务规则不明确
- 不影响核心业务流程

**行业默认做法**：
- ✅ **生产管理标准做法**：
  - 允许超产（可能因为生产损耗、工艺要求等原因）
  - 超产数量计入库存，可以用于其他订单
  - 记录超产原因（用于生产分析）
- ✅ **ERP系统做法**：超产是正常现象，系统应该支持

**建议**：
- **低优先级修复**：明确入库数量超需求处理规则
- 允许入库数量超过需求数量
- 如果入库数量 > 需求数量，任务状态仍为`completed`，但记录超产数量
- 超产数量计入库存，可以用于其他订单
- 可选：记录超产原因（用于生产分析）

---

#### 2.2.3 批次分配总和大于订单数量 ⚠️ **边界条件歧义**

**歧义描述**：
- 订单审批时进行批次分配
- 规则未明确：如果批次分配总和 > 订单数量，如何处理？
- 当前代码：应该在校验时阻止，但规则未明确

**影响范围**：
- 订单审批流程
- 批次分配逻辑

**是否阻塞实现**：❌ **否，不阻塞实现**
- 功能上可以运行（当前代码应该会阻止这种情况）
- 但业务规则不明确
- 不影响核心业务流程

**行业默认做法**：
- ✅ **订单管理标准做法**：
  - 批次分配总和应该 = 订单数量（不允许超分配）
  - 系统应该在校验时阻止超分配
  - 如果用户操作错误，应该提示并阻止
- ✅ **数据完整性要求**：批次分配总和不应该超过订单数量

**建议**：
- **低优先级修复**：明确批次分配规则
- 批次分配总和应该 = 订单数量
- 系统在校验时阻止超分配
- 如果用户操作错误，提示并阻止

---

#### 2.2.4 批次过期处理规则缺失 ⚠️ **边界条件歧义**

**歧义描述**：
- 批次有`expiry_date`（过期日期）字段
- 规则未明确：如果批次过期，如何处理？
- 当前代码：有`is_expired()`方法，但业务规则未明确

**影响范围**：
- 批次管理
- 发货流程
- 库存管理

**是否阻塞实现**：❌ **否，不阻塞实现**
- 功能上可以运行（当前代码有`is_expired()`方法）
- 但业务规则不明确
- 不影响核心业务流程

**行业默认做法**：
- ✅ **库存管理标准做法**：
  - 批次过期后，在库存列表中标记为"已过期"
  - 发货时优先使用未过期的批次（FIFO + 过期日期优先）
  - 如果只有过期批次，需要特殊审批才能发货
- ✅ **质量管理要求**：过期批次需要特殊处理，可能需要报废或降级

**建议**：
- **低优先级修复**：明确批次过期处理规则
- 批次过期后，在库存列表中标记为"已过期"
- 发货时优先使用未过期的批次
- 如果只有过期批次，需要特殊审批才能发货

---

#### 2.2.5 批次数量为0处理规则缺失 ⚠️ **边界条件歧义**

**歧义描述**：
- 批次有`quantity`（数量）字段
- 规则未明确：如果批次数量为0，是否应该删除批次？
- 当前代码：不删除批次，保留历史记录

**影响范围**：
- 批次管理
- 历史追溯

**是否阻塞实现**：❌ **否，不阻塞实现**
- 功能上可以运行（当前代码不删除批次）
- 但业务规则不明确
- 不影响核心业务流程

**行业默认做法**：
- ✅ **库存管理标准做法**：
  - 批次数量为0时，不删除批次，保留历史记录
  - 在批次列表中标记为"已用完"
  - 保留批次记录用于历史追溯和审计
- ✅ **ERP系统做法**：批次记录是历史数据，不应该删除

**建议**：
- **低优先级修复**：明确批次数量为0处理规则
- 批次数量为0时，不删除批次，保留历史记录
- 在批次列表中标记为"已用完"

---

### 2.3 体验相关歧义

#### 2.3.1 状态定义但实际不会停留 ⚠️ **体验歧义**

**歧义描述**：
- 多个状态在代码中定义，但实际流程中不会停留：
  - `SalesOrder.approved`：定义但不会停留（直接进入`ceo_pending`）
  - `SalesOrder.ceo_approved`：定义但不会停留（直接进入`in_production`或`ready_to_ship`）
  - `ProductionTask.received`：定义但不会停留（直接进入`in_production`）
  - `ProductionTask.material_preparing`：定义但实际不会停留（任务接收时直接进入`in_production`）
  - `MaterialRequisition.issued`：定义但实际不使用
  - `PurchaseTask.purchasing`：定义但不会停留（直接从`approved`到`completed`）
  - `Shipment.loading`：定义但不会停留（直接从`pending`到`shipped`）

**影响范围**：
- 状态显示
- 业务流程理解
- 报表统计

**是否阻塞实现**：❌ **否，不阻塞实现**
- 功能上可以运行，但可能导致理解困惑
- 状态列表中可能出现从未使用的状态
- 不影响核心业务流程

**行业默认做法**：
- ✅ **系统设计最佳实践**：
  - 状态定义应该与实际流程一致
  - 如果状态不会停留，应该从状态选择中移除，或明确说明是"中间状态"
  - 或者，保留状态用于历史记录（即使不会停留）
- ✅ **用户体验要求**：状态应该清晰明确，不应该有歧义

**建议**：
- **低优先级修复**：明确状态定义与实际使用的一致性
- 如果状态不会停留，应该从状态选择中移除
- 或者，明确说明这些状态是"中间状态"，用于历史记录
- 在状态列表中标记为"系统内部状态"

---

#### 2.3.2 收货回执是否必须上传图片 ⚠️ **体验歧义**

**歧义描述**：
- 收货回执可以上传图片（可选，可多张）
- 规则未明确：收货回执是否必须上传图片？
- 当前代码：图片上传是可选的

**影响范围**：
- 收货回执流程
- 数据完整性

**是否阻塞实现**：❌ **否，不阻塞实现**
- 功能上可以运行（当前是可选的）
- 但业务规则不明确
- 不影响核心业务流程

**行业默认做法**：
- ⚠️ **行业做法不一致**：
  - **严格管理**：必须上传图片（用于证据留存）
  - **灵活管理**：可选上传图片（根据业务需要）
- ✅ **最佳实践**：建议强制上传至少一张图片（用于证据留存和纠纷处理）

**建议**：
- **低优先级修复**：明确收货回执图片上传规则
- 建议：强制上传至少一张图片（用于证据留存）
- 或者，根据业务需要配置是否强制上传

---

#### 2.3.3 订单号是否保留 ⚠️ **体验歧义**

**歧义描述**：
- 订单退回后，销售员可以修改后重新提交
- 规则未明确：是否保留原订单号？
- 当前代码：应该保留订单号（订单号是唯一标识）

**影响范围**：
- 订单退回流程
- 订单追踪

**是否阻塞实现**：❌ **否，不阻塞实现**
- 功能上可以运行（当前代码应该保留订单号）
- 但业务规则不明确
- 不影响核心业务流程

**行业默认做法**：
- ✅ **订单管理标准做法**：
  - 订单号是唯一标识，应该保留
  - 订单号不应该因为退回而改变
  - 订单号用于订单追踪和审计
- ✅ **ERP系统做法**：订单号是业务主键，不应该改变

**建议**：
- **低优先级修复**：明确订单号保留规则
- 订单号应该保留（订单号是唯一标识）
- 订单号不应该因为退回而改变

---

#### 2.3.4 审批历史是否保留 ⚠️ **体验歧义**

**歧义描述**：
- 订单退回后，销售员可以修改后重新提交
- 规则未明确：是否保留审批历史？
- 当前代码：应该保留审批历史（用于审计追踪）

**影响范围**：
- 订单退回流程
- 审计追踪

**是否阻塞实现**：❌ **否，不阻塞实现**
- 功能上可以运行（当前代码应该保留审批历史）
- 但业务规则不明确
- 不影响核心业务流程

**行业默认做法**：
- ✅ **审计要求**：
  - 审批历史应该保留（用于审计追踪）
  - 即使订单退回，审批历史也应该保留
  - 审批历史是重要的审计证据
- ✅ **ERP系统做法**：审批历史是审计数据，不应该删除

**建议**：
- **低优先级修复**：明确审批历史保留规则
- 审批历史应该保留（用于审计追踪）
- 即使订单退回，审批历史也应该保留
- 重新提交后，清除之前的审批信息（`approved_by`、`approved_at`等字段清空），但保留历史记录

---

## 三、补充规则建议

### 3.1 质检不合格处理规则补充

**建议规则**：
1. **不合格品处理**（`unqualified`）：
   - 任务状态保持`qc_checking`
   - 需要创建"不合格品处理单"（可选功能）
   - 记录不合格原因和处理方式（报废、降级等）

2. **返工处理**（`rework`）：
   - 任务状态变为`in_production`
   - 允许继续生产（不需要重新领料，因为原料已领用）
   - 记录返工原因

3. **质检记录与入库单关联**：
   - 入库单可以关联质检记录，但不是必须的
   - 如果关联质检记录，应该关联结果为`qualified`的质检记录

---

### 3.2 订单退回后重新提交规则补充

**建议规则**：
1. **订单号保留**：
   - 订单号应该保留（订单号是唯一标识）
   - 订单号不应该因为退回而改变

2. **审批历史保留**：
   - 审批历史应该保留（用于审计追踪）
   - 即使订单退回，审批历史也应该保留
   - 重新提交后，清除之前的审批信息（`approved_by`、`approved_at`等字段清空），但保留历史记录

3. **重新提交流程**：
   - 重新提交后，状态从`rejected`变为`pending`
   - 需要重新走完整审批流程

---

### 3.3 入库数量超需求处理规则补充

**建议规则**：
1. **允许超产**：
   - 允许入库数量超过需求数量
   - 超产数量计入库存，可以用于其他订单

2. **任务完成判断**：
   - 如果入库数量 > 需求数量，任务状态仍为`completed`
   - 记录超产数量（`completed_quantity - required_quantity`）

3. **超产原因记录**（可选）：
   - 记录超产原因（用于生产分析）
   - 可以在入库单备注中记录

---

### 3.4 批次分配规则补充

**建议规则**：
1. **批次分配总和校验**：
   - 批次分配总和应该 = 订单数量（不允许超分配）
   - 系统在校验时阻止超分配
   - 如果用户操作错误，提示并阻止

2. **批次分配不足处理**：
   - 如果批次分配总和 < 订单数量，不足部分创建生产任务
   - 如果批次分配总和 = 订单数量，创建发货通知单

---

### 3.5 批次过期处理规则补充

**建议规则**：
1. **批次过期标记**：
   - 批次过期后，在库存列表中标记为"已过期"
   - 使用`is_expired()`方法判断

2. **发货时批次选择**：
   - 发货时优先使用未过期的批次（FIFO + 过期日期优先）
   - 如果只有过期批次，需要特殊审批才能发货

3. **过期批次处理**（可选）：
   - 过期批次需要特殊处理，可能需要报废或降级
   - 可以创建"过期批次处理单"

---

### 3.6 批次数量为0处理规则补充

**建议规则**：
1. **批次保留**：
   - 批次数量为0时，不删除批次，保留历史记录
   - 在批次列表中标记为"已用完"

2. **历史追溯**：
   - 保留批次记录用于历史追溯和审计
   - 批次记录是历史数据，不应该删除

---

### 3.7 状态定义一致性规则补充

**建议规则**：
1. **状态清理**：
   - 如果状态不会停留，应该从状态选择中移除
   - 或者，明确说明这些状态是"中间状态"，用于历史记录

2. **状态标记**：
   - 在状态列表中标记为"系统内部状态"
   - 在状态说明中明确说明这些状态的实际用途

---

### 3.8 收货回执图片上传规则补充

**建议规则**：
1. **图片上传要求**：
   - 建议：强制上传至少一张图片（用于证据留存）
   - 或者，根据业务需要配置是否强制上传

2. **图片管理**：
   - 支持多张图片上传
   - 每张图片可以添加说明（`remark`字段）

---

## 四、已优化功能总结

### 4.1 库存扣减方式统一 ✅ **已优化**

**优化内容**：
- 任务接收时：不再自动扣减库存，而是创建领料单（`status='pending'`），需要仓库管理员审批
- 领料单审批时：按批次FIFO扣减，使用`get_available_quantity()`方法（考虑锁定数量）
- 发货确认时：按批次FIFO扣减，从锁定数量中扣减

**优化效果**：
- ✅ 统一使用批次FIFO扣减
- ✅ 库存扣减方式一致
- ✅ 批次追溯完整

---

### 4.2 批次分配锁定库存 ✅ **已优化**

**优化内容**：
- `Batch`模型添加`locked_quantity`字段
- 总经理审批时：锁定批次库存（`batch.locked_quantity += order_batch.quantity`）
- 发货时：从锁定数量中扣减
- 订单终结时：释放锁定的批次库存

**优化效果**：
- ✅ 批次分配后锁定库存
- ✅ 防止超卖
- ✅ 保证订单履约

---

### 4.3 订单终结时库存回滚 ✅ **已优化**

**优化内容**：
- 已发货订单重新入库时创建新批次（批次号格式：`RETURN-{原订单号}-{日期}`）
- 已入库的成品需要扣减（按批次FIFO，从最早入库的批次开始）
- 已发料的原料需要重新入库（创建新批次，批次号格式：`RETURN-{原订单号}-{原料SKU}-{日期}`）

**优化效果**：
- ✅ 完整回滚所有库存变动
- ✅ 批次追溯完整
- ✅ 库存数据准确

---

### 4.4 发货时批次库存检查 ✅ **已优化**

**优化内容**：
- 发货前检查批次库存是否充足（计算所有批次的可用数量总和）
- 如果批次库存不足，不允许发货，提示用户

**优化效果**：
- ✅ 发货前进行库存校验
- ✅ 防止库存不足发货
- ✅ 明确的错误处理机制

---

### 4.5 客户删除规则 ✅ **已优化**

**优化内容**：
- 添加`is_deleted`字段（软删除标记）
- 添加`has_related_orders()`方法（检查是否有关联订单）
- 如果客户有关联订单，执行软删除（`is_deleted=True`）
- 如果客户无关联订单，执行硬删除（`customer.delete()`）

**优化效果**：
- ✅ 数据完整性保护
- ✅ 历史追溯完整
- ✅ 关联订单的客户信息保留

---

### 4.6 采购部分收货 ✅ **已支持**

**优化内容**：
- 支持分多次收货（每次收货时更新`received_quantity`）
- 检查是否全部收货，如果未全部收货，状态保持`purchasing`

**优化效果**：
- ✅ 支持分多次收货
- ✅ 每次收货时更新`received_quantity`
- ✅ 只有当所有明细项都收货完成时，任务状态才变为`completed`

---

## 五、总结

### 5.1 业务规则完整性评价

**已明确的规则**：
- ✅ 销售订单审批流程
- ✅ 生产任务基本流程
- ✅ 采购任务基本流程
- ✅ 物流发货基本流程
- ✅ 库存批次管理基本规则
- ✅ 订单终结时库存回滚规则（已优化）
- ✅ 发货时批次库存检查规则（已优化）
- ✅ 客户删除规则（已优化）

**待补充的规则**：
- ⚠️ 质检不合格处理规则
- ⚠️ 订单退回后重新提交规则
- ⚠️ 入库数量超需求处理规则
- ⚠️ 批次分配规则
- ⚠️ 批次过期处理规则
- ⚠️ 批次数量为0处理规则
- ⚠️ 状态定义一致性规则
- ⚠️ 收货回执图片上传规则

### 5.2 潜在风险点

1. **质检不合格处理不完整**：可能导致任务卡在`qc_checking`状态
2. **状态定义与实际使用不一致**：可能导致理解困惑和业务逻辑错误

### 5.3 建议优先级

**中优先级**：
1. 补充质检不合格处理规则

**低优先级**：
2. 明确订单退回后重新提交规则
3. 明确入库数量超需求处理规则
4. 明确批次分配规则
5. 明确批次过期处理规则
6. 明确批次数量为0处理规则
7. 明确状态定义一致性规则
8. 明确收货回执图片上传规则

---

**文档生成时间**：2026-01-14  
**分析人员**：业务 Agent  
**文档状态**：业务规则与状态分析报告（最终版），供开发参考
